coverage:
  status:
    project:
      default:
        target: auto
        threshold: 0%
    patch:
      default:
        target: auto
        threshold: 0%

comment:
  layout: "reach,diff,flags,files"
  require_changes: true
